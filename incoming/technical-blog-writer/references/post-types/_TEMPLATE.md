# <Post type name>

> Copy this file to `references/post-types/<name>.md` and fill every section. A pack with an empty section is worse than no pack, because the skill will follow it anyway.

---

## Choose this when

The conditions under which this shape is the right one. Be specific enough that 2 packs are never both correct. If the subject fits 2 packs, the tie-break rule belongs here.

## Do not choose this when

The cases where this shape produces a weak post even though it technically fits.

## Required ledger rows

The evidence that must exist in the Phase 1 truth ledger before this type can be selected. If the repository cannot supply these rows, the type is unavailable and a different one must be chosen. This constraint is the point of the pack system: it stops a shape from being filled with invention.

## Anatomy

The sections, in order, with what each one does. Say which are mandatory and which are optional. Name the section that carries the argument.

## The thesis shape

What a thesis sentence looks like for this type, with 1 example. The example uses a subject unrelated to the author's actual work, so packs stay comparable and no example leaks into a real post.

## Failure mode

The characteristic way this type goes wrong, named specifically enough to be checked for at Phase 4.5. Every type has one, and it is usually the shape collapsing into its lazier neighbour.

## Length

A rough range and what drives it.
